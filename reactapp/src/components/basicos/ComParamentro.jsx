export function ComParamentro(params) {
    return(
        <div>
            <h1>{params.titulo}</h1>
            <h3>{params.subtitulo}</h3>
        </div>

    )
}

function getRandom(params) {
    return Math.random();
  }